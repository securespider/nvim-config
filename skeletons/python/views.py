#!/usr/bin/env python

"""
Description: Implementation of django view for ${cursor}
Author: ${gh-user} (${gh-email})
Created: ${date} ${time}
"""

from django.shortcuts import render
from django.http import HttpResponse

